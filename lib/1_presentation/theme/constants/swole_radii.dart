import 'package:flutter/painting.dart';

class SwoleRadii {
  static const k5pxRadius = BorderRadius.all(
    Radius.circular(5),
  );
  static const k10pxRadius = BorderRadius.all(
    Radius.circular(10),
  );
  static const k15pxRadius = BorderRadius.all(
    Radius.circular(15),
  );
}
